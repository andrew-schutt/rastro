{
  "name": "identity-spike",
  "version": "0.0.0",
  "private": true,
  "description": "Measurement tooling for docs/VALIDATION-PLAN.md §3.2. Never shipped, never imported by the SDK.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/react": "^19.1.9",
    "@types/react-dom": "^19.1.7",
    "jsdom": "^26.1.0",
    "rastro-core": "workspace:*",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
