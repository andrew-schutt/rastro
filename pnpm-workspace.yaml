packages:
  - 'packages/*'
  - 'apps/*'
  - 'examples/*'

# better-sqlite3 compiles a native binding; esbuild fetches its platform binary (via Vite).
allowBuilds:
  better-sqlite3: true
  esbuild: true
